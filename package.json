{
  "name": "ai-product-management",
  "version": "1.0.0",
  "private": true,
  "description": "Subscription Ghost Buster monorepo",
  "scripts": {
    "dev:api": "npm --workspace services/api-relay run dev",
    "dev:web": "npm --workspace apps/family-scan-web run dev",
    "dev:mobile": "npm --workspace apps/mobile run dev",
    "lint": "npm run -ws lint",
    "test": "npm run -ws test"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
